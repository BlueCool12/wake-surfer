{
  "name": "wake-surfer",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Video conferencing and realtime collaboration platform.",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build:realtime-chat": "turbo run build --filter=@wake-surfer/realtime-chat-api --filter=@wake-surfer/realtime-chat-gateway --filter=web",
    "deploy": "node scripts/deploy.mjs",
    "dev:realtime-chat": "pnpm run deploy",
    "dev:realtime-chat:host": "pnpm docker:up && pnpm build:realtime-chat && node scripts/run-realtime-chat-dev.mjs",
    "lint": "eslint .",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "db:migrate:realtime-chat": "docker compose run --rm --build realtime-chat-migrate",
    "db:validate:realtime-chat": "docker compose run --rm --build --no-deps realtime-chat-migrate migrate validate --dir file:///workspace/migrations",
    "docker:up": "docker compose up -d realtime-chat-postgres realtime-chat-redis && docker compose run --rm --build realtime-chat-migrate",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:ps": "docker compose ps"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "prettier": "^3.9.3",
    "turbo": "^2.10.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  }
}

NODE_ENV=production
PORT=4000
LOG_LEVEL=info
MEDIA_MAX_PEERS_PER_ROOM=8
MEDIA_RTC_PORT=44444

# 컨테이너 내부 docker 네트워크 IP를 브라우저에 알려주면 연결이 조용히 실패한다(루프백 함정과
# 같은 계열). 같은 호스트에서 접속한다는 이 리포의 로컬 배포 전제를 따라 명시적으로 고정한다.
MEDIASOUP_ANNOUNCED_ADDRESS=127.0.0.1

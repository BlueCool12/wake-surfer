NODE_ENV=production
PORT=4000
LOG_LEVEL=info
MEDIA_MAX_PEERS_PER_ROOM=8
MEDIA_RTC_PORT=44444

# MEDIASOUP_ANNOUNCED_ADDRESS는 여기 두지 않는다. compose.yml의 `environment:`가 배포 시점
# 환경변수(${MEDIASOUP_ANNOUNCED_ADDRESS:-127.0.0.1})로 덮어쓸 수 있어야 하는데, env_file 값은
# 그 자리에서 고정돼 매 배포마다 이 파일을 고쳐야 하기 때문이다.
